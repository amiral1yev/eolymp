n = int(input())  # Чтение количества вершин
edges_count = 0  # Счётчик рёбер

# Чтение матрицы смежности и подсчёт рёбер
for _ in range(n):
    row = list(map(int, input().split()))  # Чтение строки матрицы
    edges_count += sum(row)  # Добавляем количество единиц в строке

# Выводим количество рёбер
print(edges_count)
