class ListNode:
    def __init__(self, x):
        self.val = x
        self.next = None

def sum_linked_list(head):
    total = 0
    current = head
    
    while current:
        total += current.val
        current = current.next
    
    return total
