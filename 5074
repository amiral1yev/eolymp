# Чтение количества вершин и рёбер
n, m = map(int, input().split())

# Инициализация массива степеней вершин
deg = [0] * n

# Чтение рёбер и подсчёт степеней
for _ in range(m):
    u, v = map(int, input().split())
    deg[u - 1] += 1  # увеличиваем степень вершины u
    deg[v - 1] += 1  # увеличиваем степень вершины v

# Вывод степеней вершин
print(*deg, sep="\n")
