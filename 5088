# Чтение входных данных
n, m = map(int, input().split())  # Читаем количество вершин и рёбер

# Инициализация списка для хранения степени вершин
degrees = [0] * (n + 1)  # Степень каждой вершины (от 1 до n)

# Чтение рёбер и обновление степеней вершин
for _ in range(m):
    u, v = map(int, input().split())  # Читаем вершины ребра
    degrees[u] += 1
    degrees[v] += 1

# Подсчет количества висячих вершин (степень равна 1)
hanging_vertices = degrees.count(1)

# Выводим результат
print(hanging_vertices)
