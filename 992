n = int(input())  # Чтение размера матрицы (количество вершин)
res = 0  # Переменная для суммы

# Чтение матрицы смежности и подсчет суммы
for _ in range(n):
    res += sum(map(int, input().split()))

# Поскольку граф неориентированный, делим сумму на 2
res //= 2
print(res)
