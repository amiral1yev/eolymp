# Чтение количества моментов времени
n = int(input())

# Список для хранения всех моментов времени
time = []

# Чтение входных данных и добавление в список
for _ in range(n):
    h, m, s = map(int, input().split())
    time.append((h, m, s))

# Сортировка списка moments по времени
time.sort()

# Вывод отсортированных моментов времени
for t in time:
    print(*t)
