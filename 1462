# Функция для сравнения чисел
def compare(a, b):
    if a % 10 == b % 10:
        return a - b  # Если последние цифры равны, сортируем по числам
    return a % 10 - b % 10  # Сортируем по последней цифре

# Чтение количества чисел
n = int(input())

# Чтение входных чисел в массив
m = [int(input()) for _ in range(n)]

# Сортировка массива с использованием кастомной функции сравнения
m.sort(key=lambda x: (x % 10, x))

# Вывод отсортированного массива
print(*m)
