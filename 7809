# Массив для подсчета количества футболок каждого цвета
m = [0] * 10

# Чтение входных данных
x_values = list(map(int, input().split()))

# Подсчитаем количество футболок каждого цвета
for x in x_values:
    m[x] += 1

# Подсчитаем количество эстетических пар
res = sum(m[i] // 2 for i in range(10))

print(res)
