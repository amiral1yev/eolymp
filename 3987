# Чтение количества вершин n и рёбер m
n, m = map(int, input().split())

# Инициализация матрицы смежности
g = [[0] * (n + 1) for _ in range(n + 1)]

# Строим матрицу смежности
for _ in range(m):
    a, b = map(int, input().split())
    g[a][b] = g[b][a] = 1  # Так как граф неориентированный

# Проверка на полный граф
flag = 1  # Флаг, который укажет, является ли граф полным
for i in range(1, n + 1):
    for j in range(i + 1, n + 1):  # Проверяем элементы над главной диагональю
        if g[i][j] == 0:  # Если хотя бы один элемент не равен 1
            flag = 0

# Выводим результат
if flag == 1:
    print("YES")
else:
    print("NO")
